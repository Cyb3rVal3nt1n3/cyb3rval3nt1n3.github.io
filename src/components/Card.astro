---
export interface Props {
  href?: string;
  class?: string;
}

const { href, class: className = "" } = Astro.props;
const Tag = href ? "a" : "div";
---

<Tag href={href} class:list={["terminal-card block", className]}>
  <slot />
</Tag>
